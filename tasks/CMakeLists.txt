# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(vrpnTaskLib)
ADD_LIBRARY(${VRPN_TASKLIB_NAME} SHARED
    ${VRPN_TASKLIB_SOURCES})
add_dependencies(${VRPN_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${VRPN_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${VRPN_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${VRPN_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VRPN_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${VRPN_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${VRPN_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VRPN_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VRPN_TASKLIB_HEADERS}
    DESTINATION include/orocos/vrpn)

